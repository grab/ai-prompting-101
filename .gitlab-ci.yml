image: node:20-alpine

cache:
  paths:
    - node_modules/
    - .next/cache/

pages:
  stage: deploy
  script:
    - npm ci
    - npm run build
    - rm -rf public
    - mv out public
  artifacts:
    paths:
      - public
  only:
    - master
    - main
